[project]
name = "intent-evidence-core"
version = "0.1.0"
description = "Dependency-free governed execution reference for AI-assisted workflows"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.setuptools]
package-dir = {"" = "src"}
