# Security policy

Do not report real credentials, tokens, private keys, personal data, private repository contents, or production endpoints in public issues.

This v0.1 is an offline reference implementation. It does not perform external writes or model calls. Any future connector or provider integration must remain explicit opt-in and must not weaken the fail-closed default.
